# 第九章：测试策略：逻辑与 UI 的平衡

很多人认为 GUI 应用很难写测试。实际上，只要架构得当（遵循 EVM 模式），测试可以变得既简单又高效。

## 9.1 测试的重心：剥离 UI

GPUI 的视图代码（Render 部分）确实难以进行纯单元测试。因此，**测试的重心应放在纯逻辑层**。

*   **模型测试**: 验证 JSON 解析是否健壮。
*   **工具函数测试**: 验证格式化、颜色转换等计算是否准确。
*   **协议测试**: 验证 HTTP 请求构建是否正确。

## 9.2 集成测试的组织

在 Rust 中，建议将集成测试放在项目根目录的 `tests/` 文件夹下。

```text
tests/
├── test_format.rs   # 测试 JSON 美化逻辑
├── test_model.rs    # 测试序列化逻辑
└── test_http.rs     # 测试网络层封装
```

## 9.3 编写网络模拟测试

在 `HiPoster` 中，我们为 `execute_request` 编写了测试用例，覆盖了：
1.  **正常响应**: 验证状态码和 Body 提取。
2.  **异常捕获**: 模拟网络断开或主机不可达，验证错误消息是否对用户友好。

## 9.4 性能基准测试 (Benchmark)

由于 GPUI 追求极致性能，对于关键路径（如大文本渲染、JSON 格式化），可以使用 `criterion` crate 进行基准测试，确保重构不会导致性能退化。

## 9.5 为什么不写 UI 测试？

在小型团队或个人项目中，UI 变化极快。维护快照测试或坐标点击测试的成本远高于收益。
**原则**: 逻辑问题靠单元测试解决，布局问题靠肉眼视觉复核。

下一章我们将进入最后一个环节：如何把这些高质量的代码打包成用户可以直接运行的安装包。
